import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 14,
  },
  components: {
    Button: {
      colorPrimary: '#1570EF'
    }
  },
};

export default theme;